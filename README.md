qml-smartslog
============

QmlSmartSlog is a QML wrapper for SmartSlog Tool.

Build
-------------

```
mkdir build
cd build
/path/to/qt/install/<version>/gcc_64/bin/qmake ..
make -j 5
make install
```
